# Installation

Development usage:

```bash
npm install
npm test
npm run build
pi -e ./src/index.ts
```

Inside Pi, run:

```text
/scaler-status
```

Project-local auto-discovery packaging will be added later.
